const app = require('./app');

app.listen(5000, () => {
  console.log('Snips server running on port 5000');
});
// app.listen(5000, () => {
//   console.log('Snips server running on port 5000');
// });
